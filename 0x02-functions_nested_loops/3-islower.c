#include "main.h"
/**
 * _islower - check if the lower case letter
 * return: 1 if c is lower case
 * return: 0 if c is not lower case
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}

}
